[Service]
# The approved corporate proxy is maintained in root's login profile.  Keep its
# address and credentials out of systemd units, runtime environment files and Git.
ExecStart=
ExecStart=/bin/bash -lc 'exec /opt/shumeyko-runtime/prod/current/.venv/bin/python -m uvicorn wb_unit_economics.web.app:app --host 127.0.0.1 --port 8097 --workers 1'
