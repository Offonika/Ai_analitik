---
spec_id: "workspace-shumeyko-tax-load-report-implementation"
title: "Налоговая нагрузка: web-сценарий и Excel"
doc_type: spec
domain: "tax-reporting"
status: accepted
owner: "engineering"
audience: ["engineering", "consultant", "operations", "accountant"]
source_of_truth: true
truth_scope: "tax-load-report"
truth_priority: 100
related_code:
  - scripts/build_onec_month_close_audit_pack.py
  - src/wb_unit_economics/config.py
  - src/wb_unit_economics/onec_odata.py
  - src/wb_unit_economics/web/models.py
  - src/wb_unit_economics/web/reports/builders.py
  - src/wb_unit_economics/web/reports/evidence.py
  - src/wb_unit_economics/web/reports/excel.py
  - src/wb_unit_economics/web/repository.py
  - src/wb_unit_economics/web/app.py
  - src/wb_unit_economics/web/static/index.html
  - src/wb_unit_economics/web/static/app.js
  - src/wb_unit_economics/web/static/styles.css
related_tests:
  - tests/test_onec_month_close_audit_pack.py
  - tests/test_multi_report_cabinet.py
  - tests/test_web_database.py
  - tests/test_web_app.py
contracts:
  - tax_load_report
ai_sections:
  status: "Статус документа"
  goal: "Goal"
  scope: "Scope"
  period: "Period Model"
  sources: "Read-Only Sources"
  methodology: "Tax Methodology Boundary"
  ui: "Web Scenario"
  reconciliation: "Reconciliation Rules"
  acceptance: "Acceptance Criteria"
  tests: "Test Plan"
  rollout: "Rollout And Rollback"
depends_on:
  - docs/specs/multi-report-cabinet-implementation.md
  - docs/decisions/2026-07-10-tax-profiles-osno-profit.md
  - docs/decisions/2026-07-14-accounting-reports-accountant-questions.md
related_specs:
  - docs/specs/month-close-control-report-implementation.md
  - docs/specs/accounting-reports-smart-process-onepage.md
supersedes: []
rollout_required: true
updated_at: "2026-07-22"
---

# Статус документа

Это accepted implementation spec staff-only advisory v1 вида `tax_load`.
Основной агрегированный показатель
фиксируется как `Налоговая нагрузка по методике ФНС`; расширенная управленческая
нагрузка со страховыми взносами или агентскими платежами не подменяет его и
может быть добавлена только как отдельный показатель. Статус `implemented`
возвращается после сквозной проверки подтвержденного evidence; missing-источники
не могут участвовать в коэффициенте.

Согласовано:

- налоговая нагрузка является самостоятельным видом отчета;
- применимые налоги и ставки определяются из налогового профиля организации 1С
  и действующих правил соответствующего режима;
- основной коэффициент считается по официальной методике ФНС;
- основной срез — календарный месяц с показателями с начала года;
- первая версия доступна `consultant/admin`;
- в первом rollout налоговые факты и клиентский вывод ведет один ответственный
  специалист, при этом подтверждение фактов и утверждение текста фиксируются
  раздельно;
- отчет можно построить как предварительный до полного закрытия месяца;
- business warnings не блокируют внутренний web-draft и Excel;
- FinKoper, автоматическая отправка и клиентская публикация из кабинета
  отложены; финальный отчет в первой версии отправляется клиенту вручную по
  принятому смарт-процессу;
- обязательный результат первой версии — web-сценарий плюс Excel.

# Goal

Собрать в одном воспроизводимом отчете налоговые начисления, платежи, ЕНС, НДС,
базы, сроки и ограничения так, чтобы консультант видел подтвержденные факты,
неподтвержденные оценки и вопросы к бухгалтеру отдельно.

Отчет не является декларацией, налоговой консультацией или платежным поручением.
Он не отправляет данные и не меняет 1С или внешние системы.

# Scope

Входит:

- `report_kind = tax_load`;
- календарный месяц и период с начала года;
- явный налоговый профиль организации;
- начислено, уплачено, задолженность/переплата и ближайшие сроки по доступным
  налогам;
- отдельный блок НДС и его подтвержденность;
- ЕНС и сверка с учетными данными;
- источники, coverage и дозапросы;
- связь со статусом закрытия месяца без жесткой зависимости для draft;
- accountant approval;
- web-представление и Excel artifact;
- advisory warnings первой версии.

# Out Of Scope

Не входит:

- подача деклараций, уведомлений и платежей;
- расчет штрафов и пеней без подтвержденной методики и источника;
- автоматический выбор налоговой оптимизации;
- подмена отсутствующих налогов нулем;
- использование AI для утверждения налоговой базы или ставки;
- поддержка всех режимов и исключений одним универсальным правилом;
- клиентская публикация до отдельного accepted решения;
- автоматическая отправка сообщения или файла клиенту;
- жесткий gate по полноте business evidence в первой версии.

# Users And Ownership

- `responsible_specialist` в первом rollout совмещает обязанности бухгалтера и
  консультанта: подтверждает налоговый профиль и факты, объясняет расхождения,
  проверяет coverage и утверждает клиентский аналитический вывод.
- Подтверждение фактов и утверждение клиентского текста остаются отдельными
  audit-действиями и могут иметь один `user_id`.
- `admin` отвечает за техническую доступность read-only источников.
- `client_*` в первой версии не получает этот отчет.

AI может подготовить объяснение только по подтвержденным расчетным полям и
обязан явно повторить ограничения предварительного отчета.

# Smart Process And Manual Delivery

ONEPAGE операционного процесса задана в
`docs/specs/accounting-reports-smart-process-onepage.md`.

Один `tax_load` связывается с отдельной клиентской задачей общей карточки
клиента, организации и календарного месяца. Финальный отчет отправляет клиенту
ответственный специалист вручную через согласованный защищенный канал. В
смарт-процессе фиксируются дата, канал, безопасное доказательство отправки и
задача контрольного контакта (`follow-up`) при отсутствии ответа.

Ручная отправка не меняет `clientPublication=false`: клиентская роль не получает
доступ к `tax_load` в кабинете, а сервис не отправляет сообщение или файл
автоматически. Ответ клиента не является условием закрытия к зарплате.

Смарт-процесс реализуется как отдельный модуль web-кабинета. Запись в FinKoper
или другую внешнюю CRM не входит в первую версию. Сроки отправки, фиксации
доказательства, контрольного контакта, эскалации и закрытия к зарплате задает
ONEPAGE; эта report spec их не переопределяет.

# Period Model

Отчет хранит два связанных периода:

- `report_period` — выбранный календарный месяц;
- `year_to_date_period` — с 1 января по конец выбранного месяца.

Месячный срез используется для текущих начислений, платежей и сроков. Срез с
начала года нужен для накопленной базы и сравнительной нагрузки. Coverage
фиксируется отдельно для каждого источника и каждого периода.

Отчет строится для явной организации 1С; current и история ведутся по связке
`tenant + client + report_kind + organization_id` по правилу spec
многотипного кабинета, поэтому отчеты разных организаций одного клиента не
вытесняют друг друга.

Если выбран январь, месячный и year-to-date диапазоны совпадают, но остаются
разными семантическими полями.

# Read-Only Sources

Предпочтительные источники:

- периодические настройки налоговой системы и НДС организации 1С из
  `InformationRegister_СистемыНалогообложенияОрганизаций` и
  `InformationRegister_НастройкиУчетаНДС`;
- ОСВ и счета налогов/ЕНС;
- read-only регистры начислений и расчетов по налогам;
- книги покупок/продаж и другие согласованные источники НДС;
- проведенные поступления/УПД услуг РВБ, их строки услуг, справочник
  номенклатуры и справочник контрагентов 1С для отдельной сверки входящего НДС
  по комиссии, логистике, хранению, приемке, продвижению и другим услугам;
- банковские и денежные движения для подтверждения платежей;
- доход по УСН без НДС из фактических поступлений на расчетный счет —
  управленческий знаменатель для ИП на УСН вместо отчета о финансовых
  результатах (подтверждено бухгалтером 21.07.2026); регистр УСН/КУДиР 1С
  используется как контрольная сверка;
- `month_close_control_report` того же клиента, организации и месяца, если он
  сформирован;
- аудируемое подтверждение бухгалтера для фактов, которых нет в read-only
  источнике.

Источник закрытия месяца является дополнительным evidence. Его отсутствие не
мешает построить предварительный `tax_load`, но влияет на статус подтверждения.

# Tax Methodology Boundary

Отчет наследует принятые правила налогового профиля:

- профиль и ставки должны быть явными;
- для ОСНО выручка и расходы приводятся к базе без НДС;
- НДС к уплате показывается отдельно и не вычитается повторно из результата;
- missing profile, rate или mandatory source оставляет показатель
  неопределенным;
- legacy-формулы применяются только к отчету с соответствующей версией
  методики.

Налоговый профиль уже является нормализованным read-only контрактом кабинета и
содержит режим, НДС, право на вычет, применимую ставку налога с выручки, период
действия и источник. Поэтому состав применимых налогов и ставок не запрашивается
у пользователя повторно.

Основной показатель называется `fns_tax_burden_ratio` и следует опубликованной
методике ФНС для оценки налогового риска:

```text
fns_tax_burden_ratio =
  paid_taxes_excluding_agents_and_insurance_contributions
  / income_excluding_participation_income
  * 100
```

В числитель входят фактически уплаченные собственные налоги. Не входят
агентские платежи, включая НДФЛ и налог на прибыль с дивидендов, а также
страховые взносы. Знаменатель для организации — доходы по отчету о финансовых
результатах без доходов от участия в других организациях. Расчет выполняется по
налогоплательщику в целом, включая его обособленные подразделения.

Нормализованные строки платежей содержат `payment_kind`,
`included_in_fns_tax_burden` и при исключении `exclusion_reason`. Если строка
не классифицирована, сумма не включается молча: сводный числитель и коэффициент
остаются `null` до уточнения.

Если отдельная расшифровка платежей на ЕНС пуста, подтвержденным fallback могут
быть проведенные и не удаленные банковские документы 1С с
`ВидОперации = Налоги` за период с начала года. Fallback применяется только
к YTD-числителю и только когда каждое назначение
однозначно классифицировано, все собственные налоговые строки сопоставлены и
для каждого класса есть ровно одна агрегированная строка отчета. НДФЛ,
страховые взносы, неоднозначные или несопоставленные документы сохраняют
числитель и коэффициент `null`.

Источники методики:

- [разъяснение ФНС о составе числителя и знаменателя](https://www.nalog.gov.ru/rn51/news/activities_fts/9962799/);
- [действующая страница ФНС с концепцией и среднеотраслевыми показателями](https://www.nalog.gov.ru/rn77/taxation/reference_work/conception_vnp/).

Налоговый кодекс и налоговый профиль определяют обязательства по каждому
налогу. Формула ФНС определяет только сводный риск-показатель и не заменяет
расчет НДС, налога на прибыль, УСН, НДФЛ или иных налогов.

Для выбранного месяца отчет показывает начисления и платежи отдельно. Основной
коэффициент считается за период с начала года и помечается `preliminary_ytd` до
завершения календарного года. В v1 сравнение с опубликованным годовым
среднеотраслевым показателем не показывается до подтверждения сопоставимости
числителей.

Таблица ФНС за 2025 год сохранена как versioned reference в
`config/reference/fns_tax_burden/2025.json`: набор фиксирует URL страницы и
файла ФНС, дату получения, SHA-256 исходного XLSX и признак включения агентского
НДФЛ в опубликованную таблицу. Live-загрузка reference при расчете запрещена;
`comparison_status = pending_methodology_confirmation` и benchmark-поля
остаются `null`.

Показатель публикуется только вместе с:

- `methodology_version`;
- `numerator_kind = paid_taxes_excluding_agents_and_insurance_contributions`;
- `denominator_kind = financial_result_income_excluding_participation_income`;
- периодом и source coverage;
- статусом полноты источников.

Если отсутствуют фактически уплаченные налоги или официальный знаменатель,
`fns_tax_burden_ratio = null`, а status объясняет source gap. Отсутствие не
заменяется нулем. Для налогоплательщика, к которому официальный знаменатель
организации неприменим, отчет не придумывает замену: нужен отдельный явно
названный показатель соответствующего режима.

Знаменатель для ИП на УСН (подтверждено бухгалтером 21.07.2026).
У ИП нет отчета о финансовых результатах, поэтому официальный организационный
`fns_tax_burden_ratio` к нему неприменим и остается `null`. Вводится отдельно
названный управленческий показатель `usn_income_tax_burden`, где знаменатель —
доход по УСН без НДС, собранный из фактических поступлений (маркетплейс-отчеты
РВБ/Озон, выкупы комиссионером, поступления на р/с, прочие доходы), как в
согласованном образце «Образец УСН доходы». При наличии регистра доходов
УСН/КУДиР из 1С он используется для сверки. Показатель помечается как
управленческий ориентир (`management_reference`) и не выдается за официальный
коэффициент ФНС организации. Если доход из поступлений не подтвержден,
`usn_income_tax_burden = null`; доступные начисления и суммы налога УСН
продолжают показываться, ноль не подставляется.

`tax_load_report` содержит:

```text
meta:
  report_id, tenant_id, client_id, report_kind
  organization_id, period_start, period_end
  ytd_start, ytd_end, methodology_version, generated_at
  calculation_status, publication_status
tax_profile:
  tax_system, profile_status, valid_from, valid_to, source_kind
source_coverage[]:
  source_kind, period_start, period_end, status, snapshot_id
tax_rows[]:
  tax_code, tax_name, period_kind
  tax_base, accrued, paid, balance, due_date
  value_status, evidence_status, source_kind, issue_code
  payment_kind, included_in_fns_tax_burden, exclusion_reason
vat_summary, ens_summary, payment_schedule
vat_books:
  period_start, period_end
  sales_status, purchase_status
  sales_rows[], purchase_rows[]:
    entry_date, counterparty_name, invoice_number, invoice_date
    vat_rate, amount_excluding_vat, vat_amount, amount_including_vat
    entry_kind, correction_status
  sales_totals, purchase_totals, vat_difference
rwb_vat_reconciliation:
  applicability, status, source_status, purchase_book_status
  period_start, period_end
  service_totals, purchase_book_totals, vat_difference
  rows[]:
    row_kind, document_date, document_number, input_number, input_date
    service_category, service_name
    amount_excluding_vat, vat_amount, amount_including_vat
    purchase_book_included, purchase_book_invoice_number, source_kind
tax_load_summary:
  metric_kind=fns_tax_risk
  numerator_kind, numerator_value
  denominator_kind, denominator_value
  fns_tax_burden_ratio, calculation_period_kind
  methodology_status, comparison_status
usn_detail:
  status, classification_status, payroll_classification_status
  source_kind, revenue_tax_rate
  income_ytd, unclassified_income_ytd, excluded_income_ytd
  loan_receipts_ytd, payroll_payments_ytd, marketplace_income_ytd
  kudir_income_ytd, reconciliation_delta
  calculated_tax_ytd, paid_tax_ytd, tax_payable, due_date
  monthly_income[]: month, value, status, row_count
  monthly_unclassified_income[]: month, value, status, row_count
  monthly_excluded_income[]: month, value, status, row_count
  monthly_loan_receipts[]: month, value, status, row_count
  monthly_payroll_payments[]: month, value, status, row_count
  monthly_marketplace_income[]: month, value, status, row_count
  marketplace_income_breakdown[]:
    category, label, value_ytd, row_count, monthly_values[]
  monthly_kudir_income[]: month, value, status, row_count
  monthly_tax_payments[]: month, value, status, row_count
issues[]:
  code, severity, section, message, next_action
business_status
accountant_approval
```

Версия сохраненного контракта с контрольными строками кредитов, маркетплейсов и
заработной платы — `tax-load-report-v6`.
В `meta` фиксируются `source_refresh_run_id`, `source_snapshot_set_id` и
`evidence_sha256`; налоговый профиль должен ссылаться на тот же refresh run или
на конкретную версию ручного override.

Числитель строится только из строк с `evidence_status=loaded|confirmed`.
Missing или неклассифицированная обязательная налоговая строка делает числитель
и коэффициент `null`. Факт уплаты берется только из явного поля подтвержденного
платежного evidence: сумма начисления или общий оборот ЕНС не считается
уплатой автоматически. Знаменатель принимается только со статусом
`loaded|confirmed` и официальным `source_kind`; иначе он и коэффициент остаются
`null`, а доступные начисления продолжают показываться.

Money and ratio values keep deterministic precision and rounding from their
accepted methodology. Отображаемое округление Excel/web не меняет сохраненный
расчет. Missing значения остаются `null`.

# Statuses And Soft Gates

`business_status` первой версии:

- `preliminary` — отчет собран, но закрытие месяца или часть evidence не
  подтверждены;
- `accountant_review_required` — факты доступны, требуется решение бухгалтера;
- `accountant_confirmed` — зарезервирован для следующего этапа после решения о
  составе обязательных источников и способе подтверждения.

Staff web view и Excel export разрешены для `preliminary` и
`accountant_review_required`. В accepted v1 `accountant_confirmed` не
выставляется ни автоматически, ни вручную.

Всегда blocking:

- tenant/access violation;
- попытка записи во внешнюю систему;
- невалидный report contract, период или организация;
- потеря lineage;
- секреты или raw client data в публичном payload/artifact registry.

Business gaps в первой версии дают warnings. Переход к жестким gates требует
изменения accepted spec, теста и rollout note.

# Web Scenario

- `Обзор` — налоговый профиль, месяц, YTD, общие суммы, business status и
  ограничения;
- `Проверки` — НДС, ЕНС, закрытие месяца, coverage и accountant review;
- `Таблицы` — налоги, начисления/платежи, календарь и расшифровки;
- `Инструкция` — роли, порядок проверки и смысл статусов.

Глобальные фильтры:

- клиент;
- вид отчета;
- календарный месяц;
- организация 1С.

Staff запускает формирование через пошаговый мастер, а не прямой action без
контекста. Мастер обязан раздельно показывать существующую ревизию выбранного
месяца и результат нового запуска, содержать явный выбор организации 1С и
месяца, показывать текущую стадию worker и давать ссылку на Excel именно по
`report_id`, созданному этим запуском. После завершения новый файл не заменяет
ссылку на существующий внутри открытого мастера; при новом открытии current
определяется заново из БД. Прямое действие скачивания бухгалтерского Excel в
общем меню скрывается, чтобы пользователь выбирал существующий или новый файл
в одном однозначном сценарии.

Карточка налоговой нагрузки не показывает процент, если формула или база не
приняты. Вместо него выводится понятный статус и следующий вопрос.

Web-представление v2 показывает бухгалтеру пользовательские русские названия,
а не имена полей контракта или внутренние status-коды. Суммы форматируются как
рубли, коэффициент — как процент, даты — в читаемом календарном формате.
Технические идентификаторы не используются как основной заголовок организации
или источника. Пустой список issues явно означает отсутствие открытых
дозапросов и не смешивается с отсутствующим evidence. Детальные таблицы должны
оставаться доступными с клавиатуры и иметь явные заголовки столбцов; на узком
экране допускается горизонтальная прокрутка без обрезания данных.

# Excel Artifact

Минимальные листы:

- `Обзор`;
- `Расчёт УСН`;
- `Налоги`;
- `График платежей`;
- `НДС`;
- `НДС РВБ`;
- `Книга продаж`;
- `Книга покупок`;
- `ЕНС`;
- `Источники и статус`;
- `Дозапросы`.

При необходимости добавляются детальные листы по налогам, но они строятся из
того же contract и не содержат raw snapshots. Workbook показывает `report_id`,
периоды, организацию, методику, coverage, business status и accountant approval.

`Книга продаж` и `Книга покупок` содержат накопительную детализацию с начала
года до конца выбранного месяца. Пользовательские колонки: дата записи,
контрагент, номер и дата счета-фактуры, ставка НДС, сумма без НДС, НДС, сумма с
НДС, вид записи и статус исправления. Контрагент определяется через read-only
справочник 1С; GUID, `Ref_Key`, `Recorder`, внутренние имена полей и коды
операций в Excel не выводятся. Ставки и признаки локализуются по-русски.

Итоги книг рассчитываются в backend и сверяются с детальными строками. Разница
между начисленным и входящим НДС показывается как информационная сверка и не
подменяет подтвержденный `НДС к уплате`. Пустая книга со статусом
`empty_expected` показывается отдельным листом с явным сообщением и
подтвержденным нулевым итогом; missing, failed или partial источник оставляет
итог `null`, а не превращает его в ноль.

`НДС РВБ` является отдельной YTD-сверкой услуг Wildberries, а не заменой
`Книги покупок`. Источник услуг — проведенные и не помеченные на удаление
поступления/УПД выбранной организации, чей контрагент подтвержден справочником
1С как `РВБ`, `Wildberries` или `Вайлдберриз`, и их строки услуг. Пользовательские
строки показывают документ и входящий номер/дату, категорию и название услуги,
сумму без НДС, НДС, сумму с НДС и факт точного сопоставления со счетом-фактурой
книги покупок. Комиссия, логистика, хранение, приемка, продвижение, эквайринг,
штрафы/доплаты и прочие услуги остаются раздельными; неизвестная номенклатура
не получает выдуманную категорию.

Итоги услуг РВБ и книги покупок рассчитываются в backend. Сводная дельта равна
`НДС книги покупок РВБ - НДС по УПД/строкам услуг РВБ`; суммы двух источников
не складываются. `matched` означает точное равенство подтвержденных итогов,
`mismatch` — расхождение, `partial_source` — неполный источник,
`empty_expected` — подтвержденное отсутствие строк. Если налоговый профиль
запрещает вычет или он неприменим, лист все равно присутствует и показывает
`Не применяется`; доступная расшифровка остается справочной и не влияет на
налоговый расчет. Missing не подменяется нулем, а сохраненный payload до v5
показывает требование повторного формирования.

Для профиля `УСН Доходы` лист `Расчёт УСН` повторяет управленческую структуру
согласованного образца в пределах подтвержденных источников: месяцы идут по
колонкам, после марта/июня/сентября/декабря показываются промежуточные итоги, а
последняя колонка содержит итог с начала года. Строки включают подтвержденные
поступления от покупателей без НДС, прочие поступления на проверке, исключенные
личные средства предпринимателя, полученные кредиты и займы, маркетплейсы,
выплаты заработной платы, итог подтвержденного дохода, контрольную базу КУДиР,
расхождение с ней, ставку УСН, исчисленный налог, подтвержденные платежи, сумму
к доплате или переплату и срок из source evidence. Квартальные и YTD значения
рассчитываются в backend из сохраненного evidence, а не скрытыми
Excel-формулами.

Подтвержденное поступление строится из проведенного и не помеченного на удаление
документа `ПоступлениеНаСчет` выбранной организации. Сумма без НДС равна сумме
строк `РасшифровкаПлатежа.СуммаПлатежа` минус
`РасшифровкаПлатежа.СуммаНДС`; сумма расшифровки обязана сходиться с
`СуммаДокумента`. `ВидОперации=ОтПокупателя` входит в подтвержденный доход,
`ЛичныеСредстваПредпринимателя` показываются отдельно и не входят в доход,
остальные виды, включая `Прочее`, показываются отдельно как требующие
классификации и не включаются в расчет молча. При таких строках статус расчета
`review_required`, но подтвержденный минимум и предварительный налог остаются
видимыми.

Полученные кредиты и займы определяются только по заполненному
`ВидОперации`, содержащему однозначный признак кредита или займа. Они
показываются отдельной строкой `Кредиты и займы полученные (не доход)` и не
увеличивают подтвержденный доход или налоговую базу УСН. Текст назначения
платежа не используется как единственный классификатор. Неизвестный вид
операции остается в `Прочие поступления без НДС (на проверке)`.

Заработная плата строится из проведенных и не помеченных на удаление документов
`РасходСоСчета`, у которых заполненный `ВидОперации` однозначно соответствует
зарплате/оплате труда. Строка `Заработная плата (выплаты, справочно)` является
контрольным денежным потоком и не уменьшает базу режима `УСН Доходы`. Если вид
операции отсутствует или сумма не подтверждена, значение остается пустым,
статус классификации — `review_required`/`source_gap`, а ноль не подставляется.

КУДиР в текущей 1С отражает движения датами налогового регистра, которые могут
быть квартальными, поэтому не используется как календарная помесячная выручка.
Она остается контрольной YTD-сверкой только по активным строкам дохода;
расходные и неактивные движения в контрольную базу не входят. Расшифровка
банковских поступлений по маркетплейсам строится через read-only справочник
`Catalog_Контрагенты`: контрагент, в наименовании которого подтверждено `РВБ`,
показывается как `Wildberries (РВБ)`, а `Интернет Решения` — как
`Ozon (Интернет Решения)`. Остальные подтвержденные покупатели показываются
отдельной строкой `Другие покупатели`; они не приписываются маркетплейсу по
назначению платежа или другому неустойчивому тексту. Помесячные строки трех
категорий обязаны сходиться с общей суммой подтвержденных поступлений. Строка
`Итого по маркетплейсам` равна backend-сумме только категорий Ozon и
Wildberries; вместе со строкой `Другие покупатели` она сходится с общими
подтвержденными поступлениями.
Отсутствующий месяц, платеж или срок остается пустым/`Не указано`, а не нулем.
Для сохраненного payload без новой банковской детализации лист показывает
старые помесячные значения только как контроль КУДиР, не подписывает их как
поступления от покупателей и явно сообщает, что отчет требует повторного
формирования. Сохраненный payload до v6 не получает нули в новых строках
кредитов, маркетплейсов и заработной платы: значения остаются пустыми, а Excel
также требует повторного формирования.

Пользовательский Excel локализуется на уровне представления: названия полей и
столбцов, статусы, виды периодов, платежей, источников и причины исключения
показываются по-русски. Внутренние ключи JSON и status-коды не становятся
заголовками или пользовательскими значениями. Табличные листы строятся по
явному allowlist пользовательских полей и не показывают `taxCode`, `issueCode`,
`snapshotId`, raw source/status-коды и другие технические колонки. ID отчёта и
версия методики сохраняются для воспроизводимости только в свойствах файла
XLSX и не занимают пользовательские ячейки; в `Обзоре` остаётся русская дата
формирования, а клиент и организация показываются пользовательскими названиями.

`Обзор` явно и раздельно показывает выбранный контрольный месяц и период
расчета `С начала года`, включая точные границы YTD, а также налоговый режим,
статус профиля, числитель, применимый знаменатель, коэффициент, business status
и статус подтверждения бухгалтера. Выбор мая означает загрузку и расчет за
`01.01–31.05`, выбор июня — за `01.01–30.06`; отдельного режима загрузки YTD нет.
Денежные значения и проценты
записываются как числовые Excel-ячейки с русским форматированием, даты — как
даты `ДД.ММ.ГГГГ`, логические значения — `Да`/`Нет`. Неизвестное enum-значение
показывается как `Не определено`, неизвестный источник не маскируется общим
названием. Служебные даты 1С вне поддерживаемого диапазона Excel, включая
`0001-01-01`, считаются отсутствующими и показываются как `Не указано`, а не
как серийное число Excel.

Каждый лист оформляется как читаемая таблица: закрепленная строка заголовков,
автофильтры, чередование строк, перенос длинного текста, разумная ширина
столбцов и параметры печати. Внешний текст из 1С всегда сохраняется как текст,
даже если начинается с формульного префикса. Файл строится во временный путь и
атомарно заменяет целевой `.xlsx`, чтобы параллельные скачивания не отдавали
частично записанный ZIP.

# Reconciliation Rules

- Начислено, уплачено и остаток не объединяются в одно поле.
- Плановая дата не считается фактическим платежом.
- ЕНС не заменяет расшифровку конкретного налога.
- НДС показывает источник и подтвержденность отдельно от расчетной оценки.
- Итоги подробных книг продаж и покупок сходятся с их строками за YTD; разница
  книг является только информационной сверкой, пока бухгалтер не подтвердил
  обязательство к уплате.
- Нельзя суммировать месячный и YTD срез как разные налоги.
- Расхождение источников остается issue и не разрешается выбором большей или
  меньшей суммы без основания.
- Сумма из `month_close_control_report` используется только при совпадении
  клиента, организации, периода и версии evidence.

# Security, Tenant Isolation And Audit

- Все автоматические подключения read-only.
- API проверяет `tenant_id`, `client_id`, `organization_id`, `report_id` и
  `report_kind`.
- Audit фиксирует методику, источники, экспорт и подтверждение бухгалтера.
- Комментарий подтверждения не содержит секреты, raw rows или клиентские URL.
- Артефакты защищены действующей моделью доступа report run.
- AI не видит данные другого tenant и не меняет tax rows или approval.

# Errors And Edge Cases

- Нет налогового профиля: суммы, зависящие от профиля, `null`; отчет остается
  preliminary.
- НДС не подтвержден: estimate и confirmed fact показываются раздельно.
- Закрытие месяца отсутствует: draft строится, но статус не может стать
  `accountant_confirmed` без отдельного ручного основания.
- Платеж виден в банке, но не распределен по налогу: оставить отдельной строкой
  с warning.
- Дата платежа вне месяца: показать фактическую дату и не переносить ее молча.
- Нет полного источника фактически уплаченных налогов или доходов: коэффициент
  `null`, доступные факты и source gap сохраняются.
- Официальный знаменатель неприменим к типу налогоплательщика: не подменять его
  управленческой выручкой под тем же названием.
- ИП на УСН: официального знаменателя из отчета о финансовых результатах нет;
  доход-база берется из поступлений без НДС как отдельно названный управленческий
  показатель `usn_income_tax_burden` (подтверждено 21.07.2026), официальный
  `fns_tax_burden_ratio` остается `null`, ноль не подставляется.
- Источник услуг РВБ доступен, а книга покупок missing/partial: показать строки
  услуг и `partial_source`, не считать их подтвержденным вычетом.
- Одна УПД содержит несколько услуг: факт попадания документа в книгу покупок
  показывается в каждой строке, но сумма книги участвует в сводном итоге один
  раз и не дублируется по категориям.
- Налоговый профиль не разрешает вычет: лист `НДС РВБ` показывает
  `Не применяется`, а не исчезает и не меняет НДС к уплате.

# Acceptance Criteria

- Report kind, клиент, организация, месяц и YTD период зафиксированы.
- Налоговый профиль имеет источник и статус.
- Начисления, платежи, balances и due dates разделены.
- Missing данные не заменяются нулями.
- Налоговая нагрузка по методике ФНС считается только из фактически уплаченных
  собственных налогов и официального доходного знаменателя.
- Агентские платежи и страховые взносы не попадают в основной коэффициент.
- Web и Excel построены из одного `report_id` и сходятся.
- Excel содержит локализованные подробные книги продаж и покупок за YTD с
  фильтрами, числовыми суммами, календарными датами и без технических ключей.
- Успешно загруженная пустая книга отличается от отсутствующего или неполного
  источника; missing не заменяется нулем.
- Excel содержит лист `НДС РВБ` с YTD-расшифровкой услуг, точным признаком
  попадания документа в книгу покупок, backend-итогами и явной применимостью
  вычета по налоговому профилю.
- Сумма НДС книги покупок РВБ не дублируется при нескольких строках одной УПД;
  расхождение остается warning и не меняет налоговый факт молча.
- Лист `Расчёт УСН` содержит строки Ozon, Wildberries, `Итого по
  маркетплейсам`, другие покупатели, полученные кредиты/займы и выплаты
  заработной платы с помесячными и YTD-значениями.
- Кредиты и займы не увеличивают доход УСН, а заработная плата не уменьшает
  базу режима `УСН Доходы`; неизвестная операция не подменяется нулем.
- Подтверждение фактов и утверждение клиентского вывода фиксируются раздельно;
  в первом rollout оба действия может выполнить один ответственный специалист.
- Staff может тестировать preliminary отчет при business warnings.
- Staff формирует отчёт через мастер с отдельными блоками существующей и новой
  ревизии; организация, месяц, стадия формирования, версия и целевой Excel
  видны до закрытия мастера.
- Security/read-only/tenant нарушения остаются blocking.
- Клиентские роли не видят отчет первой версии.

# Test Plan

- contract tests `tax_load_report`;
- month and YTD period tests;
- tax-profile applicability tests;
- missing profile/rate/source tests без zero coercion;
- parity tests формулы ФНС, исключений числителя и YTD/annual статуса;
- missing/неклассифицированный обязательный налог обнуляет числитель и ratio;
- неподтвержденный или неофициальный знаменатель не используется;
- налоговый профиль закреплен за generation refresh run или версией override;
- начислено/уплачено/balance/date reconciliation tests;
- accountant approval audit tests;
- same-user audit test: подтверждение фактов и утверждение клиентского текста
  остаются двумя событиями при одном `user_id`;
- web role and tenant-boundary tests;
- web-master tests: существующая ревизия не смешивается с новой, параметры
  организации/месяца обязательны, progress использует generation API, а новая
  download-ссылка адресует созданный `report_id`;
- web/Excel parity smoke;
- строки и итоги книг НДС, локализация ставок и контрагентов, empty- и
  missing-сценарии;
- детализация `НДС РВБ`, категории услуг, точное сопоставление УПД с книгой
  покупок, отсутствие двойного счета и профили allowed/not-applicable;
- классификация кредитов и заработной платы только по `ВидОперации`, исключение
  их из базы УСН, помесячная/YTD-сверка маркетплейсов и legacy payload до v6;
- advisory warnings allow staff draft/export;
- security violations remain blocking;
- `.venv/bin/python scripts/validate_specs.py`;
- `.venv/bin/python scripts/validate_docs_manifest.py`;
- `.venv/bin/python scripts/validate_llm_docs.py`;
- `.venv/bin/python scripts/validate_no_secrets.py`.

# Rollout And Rollback

1. Создать deterministic contract/marts поверх read-only evidence.
2. Сверить web и Excel с контрольным периодом, включая `НДС РВБ`, книгу
   покупок и применимость вычета.
3. Включить staff-only `tax_load` в advisory mode.
4. Провести несколько циклов бухгалтерской проверки.
5. Отдельно принять подтверждения, enforced gates и клиентскую публикацию.

Rollback скрывает `tax_load` в registry. Report runs и audit сохраняются;
current других видов не меняется.

## Test Rollout Evidence

- 2026-07-22: download-clarity hotfix из commit
  `b01d825cdb9f4755e390330a6565b88fa84bc068` собран в immutable release
  `runtime-b01d825-tax-download-v6-20260722` и promoted только в `test`.
  Local/public health подтвердили `status=ok`, `environment=test` и build
  `20260722-tax-load-v6-download-clarity-v1`; cwd и import path процесса
  указывают на тот же release. UI показывает текущую ревизию и время сборки,
  а имя скачиваемого налогового Excel получает суффикс `_v6`. Production
  остался на прежнем release.
- 2026-07-22: immutable release
  `runtime-26c6b9299ade-20260722-152539` из commit
  `26c6b9299ade52b6a59e1bf80fbfa1a59f808bc1` атомарно promoted в `test`;
  local и public health подтвердили `status=ok` и `environment=test`, cwd
  процесса и manifest указывают на тот же release и commit. Production не
  изменялся.
- Июньский staff-only canary завершен с контрактом `tax-load-report-v6` и
  фактическими read-only данными 1С. Excel содержит 11 листов и 19 строк
  `Расчёт УСН`, включая Ozon, Wildberries, `Итого по маркетплейсам`, другие
  покупатели, кредиты/займы и заработную плату; все три новые контрольные суммы
  получены из источников. Классификация зарплаты имеет статус `ready`, общий
  расчет корректно остается `review_required` из-за прочих поступлений. В книге
  нет формул и видимых UUID.
- 2026-07-22: immutable release
  `runtime-0c63a7a8cfb3-20260722-134917` из commit
  `0c63a7a8cfb330427bedc082e5756d50b48cc970` атомарно promoted в `test`;
  local и public `check_runtime_health.py` подтвердили `status=ok` и
  `environment=test`, а cwd/import-path запущенного процесса указывают на тот
  же release.
- Июньский staff-only `tax_load` завершен штатным canary worker с контрактом
  `tax-load-report-v5`. Повторный Excel-export подтвердил 11 обязательных
  листов, отдельный `НДС РВБ`, пользовательское `Не применяется` для УСН,
  фильтры, календарные и числовые ячейки, отсутствие формул, UUID и внутренних
  ключей в пользовательских ячейках; НДС книги покупок присутствует только в
  единственной итоговой строке и не размножается по услугам.
- До release полный `pytest -q` на implementation commit `3d01ae5` завершился
  результатом `969 passed`; последующая view-only нормализация `not_allowed` в
  `Не применяется` на `0c63a7a` прошла Ruff и `27` профильных tax-load тестов.

# Deferred Decisions

- Какие источники обязательны для статуса `accountant_confirmed`.
- Каким способом фиксируется подтверждение бухгалтера.
- Как формируется и подтверждается график платежей.
- Когда advisory checks переводятся в enforced.
- Когда и в каком виде отчет становится доступен клиенту.
- Нужен ли отдельный расширенный управленческий показатель, включающий
  страховые взносы; если нужен, он получает другое название и методику.
- Источник дохода-знаменателя для ИП на УСН — решен 21.07.2026: доход по УСН без
  НДС из поступлений (вариант «а», по образцу «Образец УСН доходы») как отдельно
  названный показатель `usn_income_tax_burden`; официальный коэффициент ФНС для
  ИП остается `null`. Реализация показателя — Фаза 2.

Вопросы, требующие ответа бухгалтера, вынесены с владельцем и сроком в
`docs/decisions/2026-07-14-accounting-reports-accountant-questions.md`.
Они блокируют `accountant_confirmed`, подтвержденный график платежей, enforced
checks и клиентскую публикацию, но не accepted staff-only advisory v1.

# Changelog

- 2026-07-22: принят пошаговый staff-master бухгалтерского отчёта по образцу
  мастера юнит-экономики: существующая ревизия, параметры нового запуска,
  progress и точная ссылка на созданный Excel показаны раздельно.
- 2026-07-22: контракт повышен до `tax-load-report-v6`; в `Расчёт УСН`
  добавлены полученные кредиты/займы, выплаты заработной платы и явный итог по
  Ozon + Wildberries. Эти контрольные потоки не меняют базу `УСН Доходы`, а
  неоднозначные операции не превращаются в ноль.

- 2026-07-22: контракт повышен до `tax-load-report-v5`; добавлен обязательный
  лист `НДС РВБ` с read-only расшифровкой УПД услуг Wildberries, сверкой с
  книгой покупок, явной применимостью вычета и запретом двойного счета.
- 2026-07-22: контракт повышен до `tax-load-report-v4`; Excel получил
  подробные YTD-листы `Книга продаж` и `Книга покупок`, backend-сверку итогов,
  русские пользовательские поля и явное различение пустого и отсутствующего
  источника.
- 2026-07-22: добавлена подтвержденная расшифровка банковских поступлений:
  `РВБ` → Wildberries, `Интернет Решения` → Ozon, остальные контрагенты →
  `Другие покупатели`; категории сверяются с общей суммой по месяцам и YTD.
- 2026-07-22: legacy-помесячные значения КУДиР больше не подписываются в новом
  Excel как банковские поступления; старый отчет явно требует повторного
  формирования.
- 2026-07-22: помесячный доход УСН переведен с квартальных дат КУДиР на
  проведенные банковские поступления без НДС; личные и неклассифицированные
  поступления показаны отдельно, КУДиР оставлена как YTD-сверка.
- 2026-07-22: принят подробный лист `Расчёт УСН` по образцу: помесячный доход
  КУДиР, квартальные/YTD итоги, ставка, исчисление, платежи и доплата без
  выдуманного разреза по маркетплейсам.
- 2026-07-22: ID отчёта и версия методики перенесены из видимого `Обзора` в
  свойства XLSX, чтобы пользовательская книга не содержала технических кодов.
- 2026-07-22: пользовательские сообщения web/Excel больше не показывают
  внутренние имена источников `onec_*` и англоязычное `read-only`; сохранённые
  payload локализуются на уровне представления без изменения lineage.
- 2026-07-21: в Excel явно разделены выбранный месяц и накопительный YTD;
  служебная дата 1С `0001-01-01` больше не попадает в пользовательскую книгу как
  число Excel.
- 2026-07-21: Excel `tax_load` переведен на пользовательский allowlist без
  технических колонок, числовые ячейки и табличное оформление с фильтрами;
  добавлены точный YTD-контекст, налоговый профиль, защита от формул и атомарная
  запись файла.
- 2026-07-21: пользовательский Excel `tax_load` получил обязательные русские
  заголовки и enum-значения; технические ключи контракта больше не выводятся как
  названия столбцов или статусы.
- 2026-07-21: при пустой расшифровке ЕНС разрешен fail-closed fallback на
  проведенные банковские документы `ВидОперации = Налоги` за YTD; неполная
  или неоднозначная классификация по-прежнему оставляет числитель `null`.
- 2026-07-21: `usn_income_tax_burden` распознает как legacy-код
  `usn_income`, так и канонический профиль 1С `УСН Доходы`; профиль
  `УСН Доходы минус расходы` не использует показатель режима «доходы».
- 2026-07-21: принято решение Фазы 0 по знаменателю для ИП на УСН (подтверждено
  бухгалтером) — доход по УСН без НДС из фактических поступлений (по образцу
  «Образец УСН доходы») как отдельно названный управленческий показатель
  `usn_income_tax_burden`; официальный `fns_tax_burden_ratio` для ИП остается
  `null`. Реализация показателя — Фаза 2.
- 2026-07-17: принят web UX v2: русские пользовательские названия вместо raw
  contract keys/status codes, форматирование денег, процентов и дат, разные
  empty states для отсутствующих дозапросов и evidence, keyboard-доступные
  таблицы и исправление отображения раздела `Проверки`.
- 2026-07-16: уточнено, что смарт-процесс является внутренним модулем кабинета,
  внешние CRM не входят в v1, а нормативные сроки и контрольный контакт задает
  ONEPAGE.
- 2026-07-16: report spec связана с accepted ONEPAGE смарт-процесса; утверждены
  ежемесячная ручная отправка финального `tax_load`, один ответственный
  специалист в первом rollout и неблокирующий ответ клиента. Клиентская
  публикация из кабинета и автоматическая отправка остаются отложены.
- 2026-07-14: код и schema v2 развернуты с выключенным `tax_load`; включение
  отложено до month-close проверки и бухгалтерской сверки числителя и
  знаменателя.
- 2026-07-14: после аудита статус возвращен в `accepted`; contract v2 должен
  закрепить evidence status, источник знаменателя и lineage налогового профиля.
- 2026-07-14: контракт повышен до `tax-load-report-v2`; подтвержденные статусы
  обязательны для числителя, явный официальный source — для знаменателя,
  missing и unclassified не подменяются нулем.
- 2026-07-14: реализованы YTD-расчет ФНС, исключения, read-only налоговый
  профиль, versioned reference 2025, staff-only web и Excel из одного payload.
- 2026-07-14: spec принята для staff-only advisory v1;
  `accountant_confirmed` зарезервирован до ответов бухгалтера.
- 2026-07-14: применимые налоги и ставки закреплены за read-only налоговым
  профилем 1С; основной коэффициент определен по методике ФНС, а вопросы о
  составе числителя и знаменателя закрыты.
- 2026-07-14: уточнения после ревью: current по организации 1С и ссылка на
  единый список оставшихся вопросов бухгалтеру.
- 2026-07-14: создан draft отдельного `tax_load`; зафиксированы месяц + YTD,
  роли бухгалтера и консультанта, предварительный запуск до закрытия месяца,
  web + Excel и мягкие business warnings первой версии.
